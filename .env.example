# Copy to .env and fill in. .env is gitignored — never commit real values.
# Real values are in your Cisco assignment PDF and in the Render dashboard.
CV_BASE_URL=https://REPLACE_WITH_CENTER_IP
CV_API_TOKEN=REPLACE_WITH_API_KEY
ALLOWED_ORIGINS=http://localhost:3000
